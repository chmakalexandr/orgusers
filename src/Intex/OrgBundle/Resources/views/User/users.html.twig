{% extends 'IntexOrgBundle::layout.html.twig' %}

{% block body %}
    <h3>
        Пользователи организации: {{ company.name }}
    </h3>
    {#<ul class="user">#}
    <div class="users">
    <div class="row">
        {% for user in users %}

            <div class="col-md-12 {{ cycle(['even', 'odd'],  loop.index0) }}">
                <a href="{{ path('intex_org_user_show', { 'user_id': user.id }) }}"> {{ user.firstname }} {{ user.lastname }} {{ user.middlename }}</a>
            </div>
            {#<li>
                <a href="{{ path('intex_org_user_show', { 'user_id': user.id }) }}"> {{ user.firstname }} {{ user.lastname }} {{ user.middlename }}</a>
            </li>#}
        {% else %}
            <p>There are no users in this company</p>
        {% endfor %}
    {#</ul>#}
    </div>
    </div>

    {% for flash_message in app.session.flashBag.get('notice') %}
        <div class="flash-notice">
            {{ flash_message }}
        </div>
    {% endfor %}



{% endblock %}